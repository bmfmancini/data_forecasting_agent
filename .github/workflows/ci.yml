name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ "**" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: "pip"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Install project requirements if they exist
        if [ -f data_forecaster/backend/requirements.txt ]; then pip install -r data_forecaster/backend/requirements.txt; fi
        if [ -f data_forecaster/frontend/requirements.txt ]; then pip install -r data_forecaster/frontend/requirements.txt; fi
        # Install development tools (pinned for reproducible CI runs)
        pip install pylint==3.3.* mypy==1.13.* black==24.10.* pytest==8.3.* pip-audit==2.7.*

    - name: Run Black (check mode)
      continue-on-error: true
      run: |
        black --check .

    - name: Run Pylint (informational)
      continue-on-error: true
      run: |
        pylint **/*.py || true

    - name: Run MyPy (informational)
      run: |
        mypy .

    - name: Run Pytest
      working-directory: .
      run: |
        pytest tests/ -v

    - name: Run pip-audit (dependency vulnerability scan)
      continue-on-error: true
      run: |
        pip-audit

    - name: Install Grype
      run: |
        curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh \
          | sh -s -- -b /usr/local/bin

    - name: Run Grype (filesystem vulnerability scan)
      continue-on-error: true
      run: |
        grype dir:. --fail-on high
