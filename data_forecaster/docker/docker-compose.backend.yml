# Backend-only deployment — run on the backend host in a distributed setup.
#
#   docker-compose -f docker-compose.backend.yml up -d --build
#
# The backend Nginx listens on BACKEND_HTTPS_PORT (default 8443) and
# terminates TLS with a self-signed certificate (auto-generated) or a
# user-supplied certificate in certs/backend/.

services:
  backend:
    build:
      context: ../backend
      dockerfile: ../docker/Dockerfile.backend
    env_file:
      - ../backend/.env
    environment:
      - FORECAST_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - OPENBLAS_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - OMP_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - MKL_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - NUMEXPR_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - BLIS_NUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - VECLIB_MAXIMUM_THREADS=${FORECAST_NUM_THREADS:-1}
      - MAX_CONCURRENT_JOBS=${MAX_CONCURRENT_JOBS:-1}
      - FORECAST_MEMORY_BUDGET_MB=${FORECAST_MEMORY_BUDGET_MB:-0}
      - FORECAST_MEMORY_HEADROOM_MB=${FORECAST_MEMORY_HEADROOM_MB:-1024}
      - THEIL_SEN_MEMORY_BUDGET_MB=${THEIL_SEN_MEMORY_BUDGET_MB:-512}
      - ARIMA_LOW_MEMORY_THRESHOLD_MB=${ARIMA_LOW_MEMORY_THRESHOLD_MB:-512}
      - FORECAST_PROCESS_ISOLATION=${FORECAST_PROCESS_ISOLATION:-true}
    mem_limit: ${BACKEND_MEMORY_LIMIT:-0}
    volumes:
      - logs_data:/app/logs
      - chroma_data:/app/chroma_db
      - file_store_data:/app/file_store
      - ../data:/app/data
      - api_key_data:/app/data/api_keys
    restart: unless-stopped
    networks:
      - forecasting_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 40s

  nginx-backend:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    environment:
      - NGINX_ROLE=backend
      - SSL_DOMAIN=${SSL_DOMAIN:-localhost}
      - BACKEND_HTTPS_PORT=${BACKEND_HTTPS_PORT:-8443}
      - BACKEND_UPSTREAM=backend:8000
    ports:
      - "${BACKEND_HTTPS_PORT:-8443}:${BACKEND_HTTPS_PORT:-8443}"
    volumes:
      - ../certs/backend:/etc/nginx/certs
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - forecasting_network
    healthcheck:
      test: ["CMD", "curl", "-kf", "https://localhost:${BACKEND_HTTPS_PORT:-8443}/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  forecasting_network:
    driver: bridge

volumes:
  logs_data:
  chroma_data:
  file_store_data:
  api_key_data:
